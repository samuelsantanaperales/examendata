1) Para cargar los cuando ejecute :

# cargar el archivo desde la pc 
uploaded = files.upload()

# Acceder al archivo cargado (asumiendo que solo cargas uno)
for filename in uploaded.keys():
    df = pd.read_csv(filename)

# mostrar los datos para comprobar 
print(df.head())


2) le da click en cargar su archivo 
3) se le despleafara una venta de windos favor de buscar su archivo guardado en su computadora y seleccionarlo.
